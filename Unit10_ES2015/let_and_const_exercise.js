var PI = 3.14;
PI = 42; // stop me from doing this!

/* Write an ES2015 Version */

const PI = 3.14;

// What is the difference between var and let?

// var is function scoped while let is block scoped. var can always be redeclared while let can only be declared once.

// What is the difference between var and const?

// const cannot be redeclared nor reassigned. Also var is function scoped while const is block scoped.

// What is the difference between let and const?

// let can be reassigned while const cannot. Both are block scoped.

// What is hoisting?

// hoisting is when the program recognizes that a variable is defined after the line of code where it being called and declares the variable prior to the call. For example, var uses hoisting which means the variable defined with var is "hoisted" to the top of the scope it is defined in.
